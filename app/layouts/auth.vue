<template>
  <!-- Clean, centered layout for the Connect screen (no sidebar). -->
  <div class="min-h-screen flex items-center justify-center p-6 bg-muted">
    <div class="w-full max-w-sm">
      <slot />
    </div>
  </div>
</template>
